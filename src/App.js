import './App.css';
import Router from "./components/Router";

function App() {
  return (
      <Router/>
      // <Routes>
      //     <Route path="/" element={<Layout />}>
      //         <Route index element={<Home/>}/>
      //         <Route path={"IphoneMobile"} element={<IphoneMobile />} />
      //         <Route path={"SamsungMobile"} element={<SamsungMobile />} />
      //     </Route>
      //     {/*<Route path="*" element={<NotFound />} />*/}
      // </Routes>
  );
}

export default App;
